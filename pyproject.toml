[project]
name = "jazz-hands"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.0",
    "setuptools>=80.1.0",
    "wheel>=0.45.1",
]
